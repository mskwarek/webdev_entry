#include <iostream>
#include <vector>
#include <algorithm>

namespace AtBashCode
{
  char codeChar(char beforeCoding);
  std::string Code(std::string ToCode);
  std::string Decode(std::string ToDecode);
}
